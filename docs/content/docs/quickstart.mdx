---
title: Quickstart
description: Create your first environment and launch a run in under 5 minutes.
---

This guide walks you through creating an environment, uploading data, and launching your first run.

## 1. Initialize your project

Navigate to your ML project directory and run:

```bash
tahuna init .
```

This initializes the project locally by writing `.tahuna/project.yaml` and scaffolding the project files Tahuna needs, including `config.yaml`, `pyproject.toml`, `uv.lock`, and `train.py` when they do not already exist.

## 2. Review the configuration

Tahuna keeps local project bindings in `.tahuna/project.yaml`:

```yaml
entrypoint: train.py
data_dir: data
output_dir: outputs
config_file: config.yaml
python_project_file: pyproject.toml
uv_lock_file: uv.lock
framework: pt
python_version: "3.11"
```

You can also review the scaffolded project files directly, especially `config.yaml` for your training config and `train.py` for your entrypoint.

## 3. Upload training data

If your run needs input data, upload it:

```bash
tahuna data upload ./training_data.json
```

You can upload multiple files. They will appear in the Storage view on the dashboard and can be bound to your environment.

## 4. Launch a run

Start a run in your environment:

```bash
tahuna run
```

Tahuna will:

1. Provision a GPU pod matching your environment spec
2. Sync your code to the pod
3. Execute your training script
4. Stream logs and metrics in real-time
5. Collect output artifacts when the run completes

## 5. Monitor from the dashboard

Open the [Tahuna dashboard](/dashboard) to see your run in the Runs view. Click on a run to see live logs, metrics charts, diagnostics, and output artifacts.

## Next Steps

- [Environments](/docs/environments) — learn about environment configuration in detail
- [Runs](/docs/runs) — understand run lifecycle and monitoring
- [Storage](/docs/storage) — manage data uploads and run artifacts
- [CLI Reference](/docs/cli) — full list of CLI commands
