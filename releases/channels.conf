# Installer channels for CLI release resolution.
# stable: explicit promotion pointer (manual update)
# nightly: moving tag updated by the manually dispatched workflow
# Set stable after the first OSS release is published.
stable=
nightly=nightly
