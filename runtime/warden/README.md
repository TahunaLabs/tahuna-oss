# Warden Runtime

`warden` is the in-pod runtime binary that materializes synced code/data, executes training, streams logs/metrics, and uploads artifacts for Tahuna runs.

Current state:

- Module scaffolded
- Runtime contract wiring pending implementation
